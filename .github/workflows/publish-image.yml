name: Publish Docker Image

on:
  push:
    branches:
      - production
      - development

jobs:
  Build-x86:
    name: Build ${{ matrix.service }} (x86)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [ 'vita-api', 'vita-mcp', 'apollo', 'chronos' ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push the image
        uses: docker/build-push-action@v4
        with:
          context: ./${{ matrix.service }}
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}:${{ github.ref_name == 'production' && 'latest-amd64' || 'dev-amd64' }}
          platforms: linux/amd64
          file: ./${{ matrix.service }}/Dockerfile

  Build-ARM:
    name: Build ${{ matrix.service }} (ARM)
    runs-on: ubuntu-24.04-arm
    strategy:
      matrix:
        service: [ 'vita-api', 'vita-mcp', 'apollo', 'chronos' ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push the image
        uses: docker/build-push-action@v4
        with:
          context: ./${{ matrix.service }}
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}:${{ github.ref_name == 'production' && 'latest-arm64' || 'dev-arm64' }}
          platforms: linux/arm64
          file: ./${{ matrix.service }}/Dockerfile

  Create-Multi-Arch-Manifest:
    name: Create the Multi-Architecture Manifest
    runs-on: ubuntu-latest
    needs: [ Build-x86, Build-ARM ]
    strategy:
      matrix:
        service: [ 'vita-api', 'vita-mcp', 'apollo', 'chronos' ]
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifest
        run: |
          docker buildx imagetools create \
            --tag ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}:${{ github.ref_name == 'production' && 'latest' || 'dev' }} \
            ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}:${{ github.ref_name == 'production' && 'latest-amd64' || 'dev-amd64' }} \
            ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}:${{ github.ref_name == 'production' && 'latest-arm64' || 'dev-arm64' }}